// EN: Fix the code in the code tab to pass this challenge (only syntax errors). Look at the examples below to get an idea of what the function should do.

// SV: Fixa koden i kodfliken för att klara denna utmaning (endast syntaxfel). Titta på exemplen nedan för att få en uppfattning om vad funktionen ska göra.

export function cubes(a: number) {
  return a ** 3;
}
