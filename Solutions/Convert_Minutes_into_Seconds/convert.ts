// EN: Write a function that takes an integer minutes and converts it to seconds.

// SV: Skriv en funktion som tar en heltal minuter och omvandlar det till sekunder.

export function convert(minutes: number): number {
  return minutes * 60;
}
