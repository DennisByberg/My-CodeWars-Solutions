// EN: Create a function that takes the age in years and returns the age in days.

// SV: Skapa en funktion som tar åldern i år och returnerar åldern i dagar.

export function calcAge(age: number): number {
  return age * 365;
}
