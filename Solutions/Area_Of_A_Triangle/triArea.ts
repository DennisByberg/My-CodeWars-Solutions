// EN: Write a function that takes the base and height of a triangle and return its area.

// SV: Skriv en funktion som tar basen och höjden på en triangel och returnerar dess area.

export function triArea(base: number, height: number): number {
  return (base * height) / 2;
}
