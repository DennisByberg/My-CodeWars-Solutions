// EN: Create a function that takes a number as an argument, increments the number by +1 and returns the result.

// SV: Skapa en funktion som tar ett nummer som argument, ökar numret med +1 och returnerar resultatet.

export function addition(num: number): number {
  return num + 1;
}
