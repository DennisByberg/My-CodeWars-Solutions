// EN: Create a function that takes an array containing only numbers and return the first element.

// SV: Skapa en funktion som tar en array som endast innehåller nummer och returnerar det första elementet.

export function getFirstValue(arr: number[]): number {
  return arr && arr[0];
}
