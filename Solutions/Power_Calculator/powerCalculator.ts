// EN: Create a function that takes voltage and current and returns the calculated power.

// SV: Skapa en funktion som tar spänning och ström och returnerar den beräknade effekten.

export function circuitPower(voltage: number, current: number) {
  return voltage * current;
}
